# TradeTable Local Validator Evidence

Observed at `2026-08-09T00:46:28Z` on the local developer machine. This file records real process and JSON-RPC observations; it is not Devnet deployment evidence.

## Versions

```text
mb-test-validator: solana-test-validator 2.2.14 (src:e2b57760; feat:798020478, client:Agave)
ephemeral-validator: magicblock-config 0.13.19
solana: solana-cli 2.2.14 (src:e2b57760; feat:798020478, client:Agave)
```

## Startup

Base command:

```bash
mb-test-validator --reset
```

ER command (the required remotes, listen port, and lifecycle are unchanged; `--no-tui` keeps the installed CLI alive in the headless command runner):

```bash
RUST_LOG=info ephemeral-validator --remotes http://localhost:8899 --remotes ws://localhost:8900 -l 7799 --lifecycle ephemeral --no-tui
```

The first ER start reached readiness, then exited because its validator identity had insufficient base funds. The local base validator funded the deterministic identity with a real local airdrop:

```text
identity: mAGicPQYBMvcYveUZA5F5UNNwyHvfYh5xkLS2Fr1mev
airdrop signature: r26QcuuAgXoj6DRkDKNrEZcbwHzguyYtFESjnvsukvc9Xw9yQmWtsEjZjXHj5t9SSGsBVHGgRK3gdtAZHfgU8tT
post-start observed balance: 10.049273994 SOL
```

## Health and funding

```text
$ solana cluster-version -u http://localhost:8899
2.2.14

$ curl --retry 10 --retry-delay 1 -fsS http://localhost:7799 -H 'content-type: application/json' -d '{"jsonrpc":"2.0","id":1,"method":"getHealth"}'
{"jsonrpc":"2.0","result":"ok","id":1}

$ solana balance -u http://localhost:8899
500000000 SOL
```

## Listening ports

```text
solana-test-validator PID 3874: TCP *:8899 (LISTEN)
ephemeral-validator PID 4365: TCP 127.0.0.1:7799 (LISTEN)
```

## Preserved logs

```text
/tmp/tradetable-base.log  1,648,763 bytes  sha256 ff1c6591c688464503ac625974b8c33024d575f482610e4e424bf2463231bfe9
/tmp/tradetable-er.log        9,926 bytes  sha256 6048ec78c82d3b532b1aee508f85d3bf65590afb3a660b565e3ab9033dc68d84
```

These hashes identify the preserved snapshots at the observation time. The live validator-owned logs may continue to grow while the processes run.
